fun main() {
    var name="Yabsera Bogale"
    println("string length "+ name.length)
    println("string can be index forexample ${name}: index of 0:${name[0]}")
    name=name.uppercase()
    println(name)
    println(name.lowercase())
    var one:Int=1
    println(one.toString().toInt())
}